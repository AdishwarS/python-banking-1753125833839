```
# Health App

This is a simple health check app built with FastAPI.

## Setup Instructions

1. Install the required packages: `pip install -r requirements.txt`
2. Run the app: `uvicorn main:app --reload`

## Testing

Run the tests with: `python -m unittest`
```